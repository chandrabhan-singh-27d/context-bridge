# AGENTS.md

This project's agent-facing guidance lives in [`CLAUDE.md`](./CLAUDE.md). The two files are kept in sync — read either, the content is identical.

Why two files? Different agent platforms expect different filenames (`CLAUDE.md` for Anthropic Claude Code, `AGENTS.md` as the emerging cross-vendor convention). Treat them as one document.
